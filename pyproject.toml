[project]
name = "stock-analyzer"
version = "0.1.0"
description = "Analysiert Aktien und stuft sie als Kaufen, Halten oder Verkaufen ein"
requires-python = ">=3.10"

[tool.ruff]
line-length = 130

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
